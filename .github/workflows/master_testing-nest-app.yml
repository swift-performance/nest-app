# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - testing-nest-app

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://swiftcontainer.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_bc7a2ec21d9d4a59880566c93dfcfc0a }}
        password: ${{ secrets.AzureAppService_ContainerPassword_1f412b54d46f4db38e810faf9c672b79 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: swiftcontainer.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_bc7a2ec21d9d4a59880566c93dfcfc0a }}/nestapp:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'testing-nest-app'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_2f6220e028f34e988fd6d18b41dc0fb4 }}
        images: 'swiftcontainer.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_bc7a2ec21d9d4a59880566c93dfcfc0a }}/nestapp:${{ github.sha }}'